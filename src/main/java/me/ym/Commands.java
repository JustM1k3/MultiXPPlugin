package me.ym;

import me.oxolotel.utils.wrapped.command.Command;
import me.oxolotel.utils.wrapped.command.sender.CommandSender;
import org.jetbrains.annotations.NotNull;

import java.util.List;

public class Commands implements Command{
    @Override
    public boolean execute(@NotNull CommandSender commandSender, @NotNull List<String> list, @NotNull List<String> list1) {
        return false;
    }
}
